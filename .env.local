DATABASE_URL="postgresql://testuser:testpassword@localhost:5432/testdb?schema=public"

# Luxdrop API Credentials
LUXDROP_API_KEY=your_api_key_here # Replace with your actual API key
BASE_LUXDROP_API_URL=https://api.luxdrop.com
# Comma-separated list of Luxdrop affiliate codes for the leaderboard
LUXDROP_LEADERBOARD_CODES=sweetflips,anothercode,topusercode # Replace with actual codes

# PROXY CREDENTIALS
# IMPORTANT: Replace [the_omitted_password] with your actual proxy password.
# Make sure .env.local is in your .gitignore file to prevent committing secrets.
PROXY_HOST=46.203.21.92
PROXY_PORT=7079
PROXY_USERNAME=ozxnqgrw
PROXY_PASSWORD=[the_omitted_password]
